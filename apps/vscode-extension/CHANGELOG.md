# Changelog

## 0.1.0

- Initial release
- Open a `.dbml` file as a draggable ERD preview
- Layout (table positions, colors, viewport) persisted to `<schema>.dbml.layout.json`
- Edit table and column `Note` values from the canvas with native undo/redo
